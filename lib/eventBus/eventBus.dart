import 'package:cloundapp/model/crm/BusinessModel.dart';

class ThemeEventBus{
  bool isLight;
  ThemeEventBus(this.isLight);
}

class ConnectEventBus{
  bool isConnect;
  ConnectEventBus(this.isConnect);
}

class CheckIsShowEventBus{
  bool isShow;
  CheckIsShowEventBus(this.isShow);
}

